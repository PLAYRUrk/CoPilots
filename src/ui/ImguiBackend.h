#pragma once
// ImguiBackend.h -- kept for build compatibility; functionality moved to XPImguiWindow.
// Each panel (ConnectionWindow, LobbyWindow, StatusHud) now owns its own
// XPLM window and ImGui context via XPImguiWindow.

namespace cp { namespace ui {
    // intentionally empty
} }
